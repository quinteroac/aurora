{
  "name": "@aurora/media",
  "private": true,
  "scripts": {
    "dev": "uv run python main.py",
    "start": "uv run uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}",
    "lint": "ruff check .",
    "test": "uv run pytest"
  }
}
